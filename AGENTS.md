## Learned User Preferences

- When implementing an attached Cursor plan, do not modify the plan file; use existing todos and mark them in progress instead of recreating them.
- Prefer merging or consolidating work from extra git worktrees into the primary local clone and continuing on the main working tree.
- Do not publish `.cursor/` to Git remotes; if it was ever committed, stop tracking it with `git rm -r --cached .cursor`—`.gitignore` alone does not untrack files already in the index.
- When choosing next work, prefer Tier 3 repurposing (omics/CREEDS/workbench integration and honest UI) over chasing historical CtD PR-AUC headlines.

## Learned Workspace Facts

- The day-to-day UI is still Streamlit (`benchmarking/dashboard.py` / `scripts/launch_dashboard.sh`); the Next.js app under `frontend/` is the in-progress migration aligned with Stitch mockups and `docs/frontend/` until parity is reached.
- For API and Next.js work, the preferred slice order is: read-only latest run from `results/` (e.g. `GET /runs/latest`) plus `/experiments` in Next before pipeline job APIs and simulation polling. Current pipeline launches flow `/simulation/parameters` -> `POST /jobs/pipeline` -> `JobManager.create()` -> `python scripts/run_optimized_pipeline.py`; jobs are in-memory, max one concurrent, timeout after 3600s, and `scripts/train_on_heron.py` is still a standalone path not wired to `JobManager`.
- The Next.js app uses **`NEXT_PUBLIC_API_URL`** for browser calls (`frontend/lib/api.ts`) and **`API_ORIGIN`** for **`frontend/next.config.mjs`** rewrites (default `http://127.0.0.1:8000`, aligned with uvicorn in the Fly combined image). For local dev on **8780**/**3780**, set both env vars to the same API base as FastAPI so rewrites and fetches stay in sync; `./scripts/dev_stack.sh` from the repo root aligns both services. Restart Next after editing `frontend/.env.local`. A common failure mode is the browser calling one port while uvicorn listens on another (`Failed to fetch`).
- Optional **ML-Intern** orchestration uses [`huggingface/ml-intern`](https://github.com/huggingface/ml-intern) as a supervising CLI while quantum execution stays in repo-owned `scripts/quantum_job_runner.py`. Wrapper: `./scripts/ml_intern_quantum_job.sh` (dry-run: `simulator-smoke --dry-run`). Install ml-intern separately (`git clone` + `uv tool install -e .`); keep IBM tokens out of prompts—use `/settings` or the config API. Runbook: `docs/upcoming-execution/EXPERIMENT_RUNBOOK.md`.
- The Next.js v2 dashboard lives under `frontend/app/v2` with `/v2/start` as the entry route. Its state model is typed URL/session projection (`buildV2Params`/`parseV2Session`) plus API-backed evidence orchestration in `frontend/lib/v2-live-data.ts`; v2 pages use live/fallback/error states and paper-aligned fallback candidates such as `Losartan -> Atherosclerosis`, `Mitomycin -> Liver cancer`, and `Ezetimibe -> Gout`. A legacy `/visualization` route still exposes lens-style toggles; v2’s evidence-focused visualization lives at `/v2/visual`. The repurposing workbench (`/v2/repurposing`, local WIP) targets compound→disease candidate pages (RNA-seq, KG, ranking, quantum benchmark) with per-target structure evidence cards (3D + pLDDT)—not protein-centric AlphaFold DB clones; next slice is `ProteinStructureEvidenceCard` + local PDB/mmCIF viewer before optional AlphaFold DB import. Backend: `middleware/repurposing_workbench.py`, `/repurposing/candidates`. RNA-seq→repurposing CLI: `scripts/run_rnaseq_counts_pipeline.py`, `scripts/run_rnaseq_quantum_benchmark.py`, `scripts/run_full_repurposing_pipeline.py` (`kg-only` vs `kg+omics`); wire CtD scores via `scripts/export_kg_qml_scores.py` → `--kg-scores`; CREEDS via `scripts/download_creeds_signatures.py` and `perturbation_layer/creeds_reversal.py` (`--creeds-reversal-method {gene_overlap,cosine}`, `--creeds-organism any`; mean-of-top-3 profile aggregation; BRCA **4/11** human match, **6/11** with `organism=any`). Run log: `results/rnaseq_repurposing_run/FINDINGS.md`. TCGA-BRCA 60 + GSE225846 external validation reproduces classical ROC-AUC **0.973** vs QSVC **0.922** (no quantum lift).
- Frontend visualization uses client-side SVG/D3 plus v2 client islands: `frontend/components/kg-graph.tsx` uses D3 force SVG, `frontend/components/quantum-circuit.tsx`/`frontend/components/v2/circuit-evidence.tsx` render circuit views, and `frontend/components/v2/molecule-viewer.tsx` dynamically imports `3dmol@2.5.4` to render `/viz/molecule` atoms/bonds. Keep heavy molecule/WebGL viewers behind client-only/dynamic boundaries with explicit fallback states.
- The researcher workflow UI uses shared continuity primitives (`frontend/components/research-session-strip.tsx`, `research-next-actions.tsx`, `api-recovery-card.tsx`) plus persisted research state in `middleware/research_store.py` (SQLite). FastAPI exposes hypothesis and experiment-history APIs (`/hypotheses`, `/experiments/history`, `/hypotheses/{id}/experiments`), and pipeline jobs can carry `hypothesis_id` plus `experiment_metadata`. IBM Quantum settings now live at `/settings` and `/v2/settings` using `frontend/components/ibm-quantum-settings.tsx`; tenant-scoped credentials persist through `middleware/integration_store.py` and `/config/ibm-quantum` APIs, with Fernet via `INTEGRATION_ENCRYPTION_KEY` or local/dev base64 fallback.
- **Fly.io combined image** (root `Dockerfile`): Next.js listens on **0.0.0.0:$PORT** (default **8080**, aligned with `fly.toml` `internal_port`); **FastAPI** stays on **127.0.0.1:8000** and is reached via Next rewrites—not the same as local dev’s **8780/3780** split. That image installs Python deps from **`requirements-huggingface.txt`**; keep **Qiskit 1.x** there—**Qiskit 2** removes `Sampler` from `qiskit.primitives` and breaks imports in `quantum_layer/qml_model.py`.
- A **Gradio “HF lite”** shell (`hf_space/app.py`) reuses the same FastAPI app as the main stack; run it locally with `./scripts/run_hf_lite.sh` (default **7860**) when you want a lightweight researcher-facing UI instead of Next.js or Streamlit.
- **DGX Spark / Tier 1:** full-graph RotatE via `./scripts/run_full_embedding_dgx.sh` (`config/quantum_config_dgx.yaml`; `docs/deployment/DGX_SPARK.md`, `docs/roadmap/04_quantum_scaling.md` with `HYBRID_QML_SYSTEM=dgx`). Tier 1 paper blockers (MoA, CpD, multiseed) runbook: `docs/deployment/DGX_SPARK_TIER1_RUNS.md`; cached queue: `./scripts/run_tier1_cached_sequential.sh` (symlinks `rotate_256d_*` as `rotate_128d_*`). Repro runners: `run_improved_rerun.sh` (MoA + full classical), `run_256d_moa_multiseed.sh` (256D+MoA+stacking fix), `run_headline_repro.sh` (README 0.7987 attempt). TABLE3 draft: `results/multiseed/TABLE3.md`. GPU OOM is usually PyKEEN rank eval—not training—see caps in `kg_layer/advanced_embeddings.py`; do not run two `run_optimized_pipeline.py` jobs in parallel (shared `data/pykeen_tmp/train.tsv`). `scripts/aggregate_multiseed.py` reads `test_metrics.pr_auc` (includes HistGBDT). Ensemble stacking picks best PR-AUC classical (`_select_best_ensemble_classical`; HistGBDT on current runs)—`predictions_compare.csv` uses the same pick. `--fast_mode` trains only RF+ET and caps embedding epochs at 50; avoid for HistGBDT/MoA benchmarking. MoA lifts HistGBDT ~+0.03; best reproducible single-run ensemble **0.7805** (256D+MoA, seed 42; fresh 128D no-cache matches). Multiseed MoA mean **0.7398±0.038** (`rerun_256d_moa`). RF ~0.47 on current code (historical README 0.7838 won't reproduce). `optuna_pipeline_search.py` ensemble HPO (cached+MoA, 20 trials) topped **0.5861** vs manual **0.7805**; see `results/headline_repro_fresh/INVESTIGATION.md`. VQC accepts only `ZZ`/`Z` in `quantum_layer/qml_model.py` (`Pauli` is QSVC-only). Qiskit Aer GPU may fall back to CPU on ARM64.
- Prioritized experiment and product backlog notes live under **`docs/roadmap/`** (see `00_INDEX.md` and `01`–`06` topic files). README follows the Markdown Visual System template (reading order, inline mermaid, five-layer architecture). Omics path is counts-first: committed `single_cell_layer/` and `perturbation_layer/` (wet-lab FASTQ out of scope; some RNA-seq scripts local-only). OpenFold/structure is Tier 4 per `06_open_source_biomolecular_prediction.md`: local `structure_layer/` artifact ingestion + pLDDT features exist (WIP, uncommitted) but no OpenFold runner—artifact-first policy. The primary classical feature vector for 128D RotatE is **1177** dimensions (**1187** with MoA) with graph and domain features—keep Methods text aligned with `kg_layer/enhanced_features.py` when editing (`docs/paper.tex` is another narrative reference).
- **Two-paper strategy:** `docs/paper_qGG_full_2026.tex` is the **full preprint** (arXiv / journal, `extarticle` 9pt, `\thebibliography`—no bibtex pass). The **QCE short paper** is `docs/qgg_biomedical_qce.tex` → `docs/qgg_biomedical_qce.pdf` (~6 pp, short-track submit); full one-column reference build is `docs/qgg_biomedical.tex` → `docs/qgg_biomedical.pdf` (~12 pp). Shared body: `docs/qgg_biomedical_content.tex` with `\ifqceshort`; `docs/paper_qGG_qce26.tex` is **not in the repo**. Headline numbers: **0.7805** (seed 42) and **0.7398±0.038** multiseed; legacy **0.7987** is `fast_mode`/RF-led only. Tier 3 RNA-seq repurposing Methods: `docs/repurposing/RNASEQ_REPURPOSING_METHODS.md`. Build PDFs: `./scripts/build_papers.sh` → all three paper PDFs; `./scripts/build_poster.sh` → `docs/poster/poster.pdf` (A0 tikzposter; omits `hyperref` for TeX Live 2026—use `url`). Both scripts fall back to Docker `texlive/texlive:latest` when host `pdflatex` is missing. Figures regenerate with `.venv/bin/python figures/fig{1,2,3,4}_*.py`. Public repository: https://github.com/Quantum-Global-Group/hybrid-qml-kg-poc
