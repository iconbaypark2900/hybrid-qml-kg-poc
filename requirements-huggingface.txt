# Slim requirements for Hugging Face Spaces (dashboard only).
# Use this in your Space to speed up build and avoid OOM.
# See docs/deployment/DEPLOY_HUGGINGFACE.md for deployment steps.

streamlit>=1.32.0
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
scikit-learn>=1.3
joblib>=1.3
PyYAML>=6.0
networkx>=3.0

# Quantum kernel (statevector) for Live Prediction.
# Pin Qiskit 1.x: Qiskit 2+ removes `Sampler` from `qiskit.primitives` (breaks qml_model / orchestrator import chain).
qiskit==1.4.4
qiskit-aer==0.17.2
qiskit-algorithms==0.4.0
qiskit-machine-learning==0.8.4

# Optional: IBM runtime only needed for "Hardware readiness" page
# qiskit-ibm-runtime==0.42.0
