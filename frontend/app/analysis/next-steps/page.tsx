import { PagePlaceholder } from "@/components/page-placeholder";

export default function NextStepsPage() {
  return (
    <PagePlaceholder
      title="Analysis: next steps"
      description="Recommendations from last run (mockup: analysis_next_steps). API TBD."
    />
  );
}
