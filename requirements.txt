# Dashboard-only deps (HF Spaces + lightweight local runs).
# Full pipeline (PyTorch, PyKeen, etc.): pip install -r requirements-full.txt

streamlit>=1.32.0
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
scikit-learn>=1.3
joblib>=1.3
PyYAML>=6.0
networkx>=3.0

# Quantum kernel (statevector) for Live Prediction
qiskit>=1.0
qiskit-machine-learning>=0.7
qiskit-aer>=0.12
